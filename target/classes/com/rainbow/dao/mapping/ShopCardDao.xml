<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rainbow.dao.ShopCardDao">

    <select id="ShopCardList" resultType="ShopCard">
		select 
			s.shop_card_id shopCardId, 
			s.user_id userId,
			s.product_id productId,
			s.count count,
			s.type type,
			p.product_name productName,
			p.price price 
		from product p ,shop_card s where s.user_id=#{s.useId} and p.product_id=s.product_id
	</select>
	 
	<insert id="AddShopCard">
		insert into shop_card(product_id,user_id,count,type) 
		values(#{productId},#{userId},#{count},"1")
	</insert>

</mapper>